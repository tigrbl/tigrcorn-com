services:
  tigrcorn-com:
    build: .
    container_name: tigrcorn-com
    restart: unless-stopped
    labels:
      site.host: tigrcorn.com
      site.kind: mdwrk-lander
    ports:
      - "127.0.0.1:0:80"
