name: Deploy

on:
  workflow_dispatch:

concurrency:
  group: deploy-tigrcorn-com
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: deployment
    env:
      NPM_BASE_URL: ${{ secrets.NPM_BASE_URL }}
      NPM_IDENTITY: ${{ secrets.NPM_IDENTITY }}
      NPM_SECRET: ${{ secrets.NPM_SECRET || secrets.NPM_PASSWORD || secrets.NGINX_PROXY_MANAGER_SECRET }}
      NPM_TIMEOUT_S: 30
      NAMECHEAP_API_USER: ${{ secrets.NAMECHEAP_API_USER }}
      NAMECHEAP_API_KEY: ${{ secrets.NAMECHEAP_API_KEY }}
      NAMECHEAP_USERNAME: ${{ secrets.NAMECHEAP_USERNAME }}
      NAMECHEAP_CLIENT_IP: ${{ secrets.NAMECHEAP_CLIENT_IP }}
      SITE_SERVER_IP: ${{ secrets.SITE_SERVER_IP || secrets.SERVER_IP || secrets.DEPLOY_SERVER_IP }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: npm ci
      - run: npm run check
      - run: npm run build
      - name: Validate Docker proxy target
        run: npm run proxy:target-check
      - name: Build site image
        run: docker-compose -f docker-compose.yaml build tigrcorn-com
      - name: Restart site container
        run: |
          docker-compose -f docker-compose.yaml stop tigrcorn-com || true
          docker-compose -f docker-compose.yaml rm -f tigrcorn-com || true
          docker rm -f tigrcorn-com || true
          docker-compose -f docker-compose.yaml up -d --no-build --force-recreate --no-deps tigrcorn-com
      - name: Install npmctl from PyPI
        run: python -m pip install --upgrade "npmctl>=0.3.8" "npmctl-namecheap>=0.3.8"
      - name: Validate npmctl orchestration secrets
        shell: bash
        run: |
          missing=0
          for name in NPM_BASE_URL NPM_IDENTITY NPM_SECRET NAMECHEAP_API_USER NAMECHEAP_API_KEY NAMECHEAP_USERNAME NAMECHEAP_CLIENT_IP SITE_SERVER_IP; do
            if [ -z "${!name}" ]; then
              echo "Missing required secret-backed environment variable: ${name}"
              missing=1
            fi
          done
          exit ${missing}
      - name: Diagnose npmctl API authentication
        shell: bash
        run: |
          echo "NPM_BASE_URL length=${#NPM_BASE_URL}"
          echo "NPM_IDENTITY length=${#NPM_IDENTITY}"
          echo "NPM_SECRET length=${#NPM_SECRET}"
          npmctl doctor
      - name: Render DNS desired state
        shell: bash
        run: |
          mkdir -p .tmp
          sed "s/__SITE_SERVER_IP__/${SITE_SERVER_IP}/g" desired-state/dns.yaml > .tmp/dns.yaml
      - name: Validate DNS desired state
        run: npmctl validate .tmp/dns.yaml
      - name: Validate Namecheap provider
        run: npmctl dns doctor --provider namecheap
      - name: Normalize Namecheap DNS records
        run: python scripts/namecheap-normalize-dns.py .tmp/dns.yaml
      - name: Apply Namecheap DNS with npmctl
        continue-on-error: true
        run: npmctl apply .tmp/dns.yaml --owner tigrcorn-com
      - name: Validate npmctl proxy host and certificate desired state
        run: npmctl validate desired-state/proxy.yaml
      - name: Check Nginx Proxy Manager API schema
        run: npmctl schema check || echo "NPM schema check reported limited endpoint permissions; continuing with proxy-host deploy."
      - name: Adopt matching unmanaged NPM resources
        run: npmctl adopt desired-state/proxy.yaml --owner tigrcorn-com --force
      - name: Plan Nginx Proxy Manager proxy host and certificates with npmctl
        run: npmctl plan desired-state/proxy.yaml --owner tigrcorn-com
      - name: Apply Nginx Proxy Manager proxy host and certificates with npmctl
        run: npmctl apply desired-state/proxy.yaml --owner tigrcorn-com
